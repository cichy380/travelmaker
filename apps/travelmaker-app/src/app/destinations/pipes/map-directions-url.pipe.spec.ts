import { MapDirectionsUrlPipe } from './map-directions-url.pipe';

describe('MapDirectionsUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new MapDirectionsUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
