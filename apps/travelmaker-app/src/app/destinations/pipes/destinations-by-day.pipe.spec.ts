import { DestinationsByDayPipe } from './destinations-by-day.pipe';

describe('DestinationsByDayPipe', () => {
  it('create an instance', () => {
    const pipe = new DestinationsByDayPipe();
    expect(pipe).toBeTruthy();
  });
});
