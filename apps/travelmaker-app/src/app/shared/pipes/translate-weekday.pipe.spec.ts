import { TranslateWeekdayPipe } from './translate-weekday.pipe';

describe('TranslateWeekdayPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateWeekdayPipe();
    expect(pipe).toBeTruthy();
  });
});
